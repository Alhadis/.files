#!/bin/sh


folder=$(echo $1 | sed 's/s$//')
folder+="s"
shift

# Safety check
[ -d "$*" ] || {
	>&2 echo "Not a directory: $*"
	exit 2;
};

name=$(basename "$*")
dsclean
zip -r "$name.zip" "$name"
rm -rf "$name"
mv "$name.zip" "$name"
dsclean
